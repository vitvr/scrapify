// Use this for setting UI colors
import "package:flutter/material.dart";

const dark = Color.fromRGBO(255, 99, 61, 1.0);
const light = Color.fromRGBO(255, 141, 113, 1.0);
const lighter = Color.fromRGBO(255, 188, 171, 1.0);
